type Query {
    seeFeedPost(limit:Int, offset:Int): [Post!]!
    seeFeedReview(limit:Int, offset:Int): [Review!]!
    # seeFeed(limit:Int, offset:Int): Feed!
}

# type Feed {
#     posts: [Post!]!
#     reviews: [Review!]!
# }