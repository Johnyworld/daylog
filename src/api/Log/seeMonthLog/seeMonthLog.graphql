type Query {
    seeMonthLog(username:String!, yyyymm:String!): MonthLog!
}

type MonthLog {
    weekReviews: [Review!]!
    weekComments: [Comment!]!
    averageScore: Float!
    doingLogs: [DoingLog!]!
}